// * Result Enum :

//              ? The Rust enum models an operation that could be successful or erroneous...
//              ? Two example of functions/methods that return Result include stdin().read_line and File::open.
//              ? The match statement foces a pattern for every enum varient...
//              ? The Ok varient stores the error data..
//              ? The if let statement declares conditional logic for a single enum varient....
